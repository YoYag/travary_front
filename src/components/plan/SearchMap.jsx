import React from "react";

const SearchMap = () => {
  return (
    <div className="w-full h-full relative">
      <button className="btn btn-sm w-full absolute bottom-0">
        일정에 추가
      </button>
    </div>
  );
};

export default SearchMap;
